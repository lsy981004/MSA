server:
  port: 8888

spring:
  application:
    name: config-service
  rabbitmq:
    host: 127.0.0.1
    port: 5672
    username: guest
    password: guest
  profiles:
    active: native
  cloud:
    config:
      server:
        native:
          search-locations: file://${user.home}/IdeaProjects/native-file-repo
        git:
          uri: https://github.com/lsy981004/spring-cloud-config
#          username: [your username]
#          password: [your password]
#          uri: file://C:\Users\DIR-N-114\IdeaProjects\git-local-repo


management:
  endpoints:
    web:
      exposure:
        include: health, busrefresh
